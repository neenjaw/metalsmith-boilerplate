{{> doc-head }}

<body>
    <div class="wrapper">
        {{> header }}

        <main>

            <h1>{{ title }}</h1>
            <h3>{{ description }}</h3>
            
            {{{ contents }}} 
            
            {{> blog-nav }}

        </main>

        {{> footer }}

    </div>

    {{> script }}
</body>

</html>